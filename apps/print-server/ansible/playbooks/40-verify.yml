---
- name: Verify the print server stack
  hosts: all
  gather_facts: false
  tasks:
    - name: Verify print server services
      ansible.builtin.import_role:
        name: print-server-verification
      when: node_domain_role == platform_zones.bak.role
