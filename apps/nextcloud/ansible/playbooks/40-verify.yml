---
- name: Verify the multisite Nextcloud stack
  hosts: all
  gather_facts: false
  vars_files:
    - ../../images.lock.yml
  tasks:
    - name: Verify Nextcloud runtime state
      ansible.builtin.import_role:
        name: nextcloud-verification
