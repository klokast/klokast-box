---
- name: Verify torrent runtime
  hosts: torrent_app_vms
  gather_facts: false
  tasks:
    - name: Verify torrent runtime services and policy
      ansible.builtin.import_role:
        name: torrent-runtime-verification
