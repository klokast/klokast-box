---
- name: Converge or verify shared Podman Xen guest runtime intent
  hosts: vm_dom0
  gather_facts: false
  tasks:
    - name: Apply shared Podman Xen guest runtime intent
      ansible.builtin.import_role:
        name: shared-xen-guests
