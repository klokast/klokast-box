---
- name: Set up signed VM update policy evidence on the active controller
  hosts: localhost
  connection: local
  gather_facts: false
  become: true
  become_method: doas
  roles:
    - role: platform-update-authority
